---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "growthbook_project Resource - growthbook"
subcategory: ""
description: |-
  A GrowthBook project. Projects organize feature flags, experiments, metrics, and other resources, and back project-scoped permissions.
---

# growthbook_project (Resource)

A GrowthBook project. Projects organize feature flags, experiments, metrics, and other resources, and back project-scoped permissions.

## Example Usage

```terraform
resource "growthbook_project" "web" {
  name         = "Web"
  description  = "Web application feature flags and experiments"
  stats_engine = "bayesian"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Human-readable project name.

### Optional

- `description` (String) Optional description of the project.
- `stats_engine` (String) Default statistics engine for the project: `bayesian` or `frequentist`.

### Read-Only

- `date_created` (String) Creation timestamp (RFC3339).
- `date_updated` (String) Last update timestamp (RFC3339).
- `id` (String) Unique project identifier (e.g. `prj_...`).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Projects are imported by their GrowthBook ID.
terraform import growthbook_project.web prj_abc123
```
