---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "growthbook_environment Resource - growthbook"
subcategory: ""
description: |-
  A GrowthBook environment (e.g. production, staging). Environments control where feature flag rules are evaluated.
---

# growthbook_environment (Resource)

A GrowthBook environment (e.g. `production`, `staging`). Environments control where feature flag rules are evaluated.

## Example Usage

```terraform
resource "growthbook_environment" "production" {
  id             = "production"
  description    = "Production environment"
  default_state  = true
  toggle_on_list = true
  projects       = [growthbook_project.web.id]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) The environment identifier/name. Changing this forces a new environment.

### Optional

- `default_state` (Boolean) Default enabled state for new features in this environment.
- `description` (String) Description of the environment.
- `parent` (String) An environment to inherit feature rules from (requires an enterprise license).
- `projects` (List of String) Project IDs this environment is scoped to. Empty means all projects.
- `toggle_on_list` (Boolean) Show this environment as a toggle on the feature list page.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Environments are imported by their ID (name).
terraform import growthbook_environment.production production
```
